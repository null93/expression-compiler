#include <iostream>
#include <cmath>

using namespace std;

int main()
{
	double a;
	cin >> a;
	double b;
	cin >> b;
	double c;
	cin >> c;
	double d;
	cin >> d;Test
	double X;
	X = ((((((((6+4)+5)+pow(c,4))-33)-2)+5)+pow(b,pow(c,pow(((((b+((b*b)*3))-4)-pow(5,(((2*a)-d))))),6))))-((pow(3,2)*c)*c));
	cout << "And the answer is ";
	cout << X;
	cout << "\n";
}
