#include <iostream>
#include <cmath>

using namespace std;

int main()
{
	double a;
	cin >> a;
	double b;
	cin >> b;
	double c;
	c = ((a-(b/b))+a);
	cout << "The length of the hypotenuse is ";
	c = pow(((pow(a,2)+pow(b,2))),0.5);
	cout << c;
	cout << "\n";
}
